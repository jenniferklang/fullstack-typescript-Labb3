name: Run Tests

on:
  push:
    branches:
      - main

jobs:
  cypress-run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cypress.io
        uses: cypress-io/github-action@v6
        with:
          start: npm start
