name: Cypress Tests

on:
  push:
    branches:
      - main

jobs:
  cypress-run:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify Current Directory
        run: |
          pwd

      - name: Install Dependencies and Start
        run: |
          cd fullstack-ish-typescript
          npm install
          npm run dev

      - name: Run Cypress Tests
        run: |
          cd fullstack-ish-typescript/frontend
          npx cypress open
